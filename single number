public class SingleNumber {
    public static int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) {
            result ^= num; // XOR cancels out duplicates
        }
        return result;
    }

    public static void main(String[] args) {
        int[] nums1 = {2, 2, 1};
        int[] nums2 = {4, 1, 2, 1, 2};
        int[] nums3 = {1};

        System.out.println("Single Number in nums1: " + singleNumber(nums1)); // 1
        System.out.println("Single Number in nums2: " + singleNumber(nums2)); // 4
        System.out.println("Single Number in nums3: " + singleNumber(nums3)); // 1
    }
}
